# Branding page preview

Static snapshot of the Halo Lab Branding service page, published only so the
work in progress can be reviewed without a Cloudflare Access login.

- Source of truth: `Halo-Lab/halo-website`, branch `codex/branding-prefit-technical-foundation`.
- Contains just the built page and the assets it references, not the whole site.
- `robots.txt` and a page-level `noindex` keep it out of search results.
- Delete this repository once the preview is no longer needed.
